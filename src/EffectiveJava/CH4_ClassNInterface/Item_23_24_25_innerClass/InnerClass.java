package EffectiveJava.CH4_ClassNInterface.Item_23_24_25_innerClass;

public class InnerClass {

    // 1. AbstractFigure, 내부 클래스를 계층구조로 변경하라

    // 2. 내부클래스는 외부 참조가 없다면 Static으로 만들어라 ** GB가 외부참조 때문에 외부객체를 회수할 수 없어 OME 발생 가능

    // 3. 톱레벨 클래스 ( INNER가 아닌 ) 는 소스파일 당 하나만 선언하라 .
    // 이는 컴파일 오류로는 드러나지 않고 같은 이름의 메서드를 삭제해 버린다

}
